package Controller;

public class MazeCollections {
    //this is probs not needed as this would be used if we were not using a database and this would be the storage class
    //would could implement this as a class between the GUI and IMaze and have a IMaze object in here to add/delete for database etc
}
